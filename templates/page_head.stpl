<% use crate::config::CONFIG; %>
<header id="header-bg">
    <div id="header-inner">
        <h1 id="site-title"><a href="/"><%= CONFIG.title %></a></h1>
        <% if let Some(tagline) = CONFIG.tagline() { %>
            <p id="site-tagline"><%= tagline %></p>
        <% } %>
    </div>
</header>